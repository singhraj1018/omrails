<!DOCTYPE html>
<html>
  <head>
    <title>DewCt</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <!-- Navigation Bar -->
    <nav class="navbar navbar-inverse navbar-toggle-sm bg-inverse">
      <div class="container">
        <ul class="nav pull-right">
          <%= link_to "One Month Rails", root_path, class: "navbar-brand" %>
          <li class="nav-item"><%= link_to "Home", root_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "About", about_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Contact",contactus_path, class: "nav-link" %></li>
        </ul>
        <ul class="nav pull-right">
        <% if user_signed_in? %>
        <%= link_to "Log Out",signout_path, method: :delete,  class: "nav-link" %></li>
        <% else %>
        <%= link_to "Sign In",signin_path, class: "nav-link" %></li>
        <%= link_to "Sign Up",signup_path, class: "btn btn-primary" %></li>
        <% end %>
      </ul>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% flash.each do |name, message| %>
      <%= content_tag(:div, message, class: "alert alert-info #{name}") %>
    <% end %>

    <!-- Page Content -->
    <%= yield %>
    
    <!-- Footer -->
    <footer class="small text-muted">
      <div class="container">
        <hr>
        <center>
          <p>© 2016 One Month</p>
        </center>
      </div>
    </footer>

  </body>
</html>
